from sys import float_info
import cities
from util import measure_memory, measure_time

def shortest_path(city_network: cities.CityNetwork, starting_city = 0, if_admissable = True, print_steps = False):
    path = [starting_city]
    visited_cities = set()

    

def minimum_spanning_tree(): # Admissable heuristic commonly used to solve tsp
    return